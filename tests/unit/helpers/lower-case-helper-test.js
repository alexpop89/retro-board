
import { lowerCaseHelper } from 'retro-board/helpers/lower-case-helper';
import { module, test } from 'qunit';

module('Unit | Helper | lower case helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lowerCaseHelper([42]);
  assert.ok(result);
});

