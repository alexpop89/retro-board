import cleanUrlUtil from 'retro-board/utils/clean-url-util';
import { module, test } from 'qunit';

module('Unit | Utility | clean url util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cleanUrlUtil();
  assert.ok(result);
});
