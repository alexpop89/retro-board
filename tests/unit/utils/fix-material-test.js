import fixMaterial from 'retro-board/utils/fix-material';
import { module, test } from 'qunit';

module('Unit | Utility | fix material');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fixMaterial();
  assert.ok(result);
});
