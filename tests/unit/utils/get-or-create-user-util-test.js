import getOrCreateUserUtil from 'retro-board/utils/get-or-create-user-util';
import { module, test } from 'qunit';

module('Unit | Utility | get or create user util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getOrCreateUserUtil();
  assert.ok(result);
});
