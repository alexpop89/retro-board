import signIn from 'retro-board/utils/sign-in';
import { module, test } from 'qunit';

module('Unit | Utility | sign in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = signIn();
  assert.ok(result);
});
